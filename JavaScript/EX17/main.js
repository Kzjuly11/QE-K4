
function count(str, x) {
    return str.split(x).length -1  ;
  }
  
  console.log(count("the quick brown fox jumps over the lazy dog", "The")); // 2

  // từ The với the # nhau , chưa giải quyết dc 